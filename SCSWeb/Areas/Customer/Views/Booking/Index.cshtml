@* Project: CSC Mock Test
   Controller: Booking
   View      : Index
   Author    : Pedro Martinez
*@

@* @using SCS.Models.ViewModels *@
@model Product

<form method="post">
    <br />
    <div class="card border-1">
        <div class="card-header bg-primary bg-gradient text-light ml-0 py-4">
            <div class="row px-4">
                <div class="col-6">
                    <h5 class="pt-2 text-white">
                        Certification Vouchers
                    </h5>
                </div>
             </div>
        </div>

        @* Table *@
        <table class="table table-bordered  table-striped table-light" style="width:100%">
            <thead>
                <tr>
                    <th class="text-success-emphasis">
                        Name
                    </th>
                    <th class="text-success-emphasis">
                        Category
                    </th>
                    <th class="text-success-emphasis">
                        Provider
                    </th>
                    <th class="text-success-emphasis">
                       Price
                    </th>
                    <th class="text-success-emphasis">
                        Certification Slot
                    </th>
                    <th class="text-success-emphasis">
                       Status
                    </th>
                </tr>
            </thead>
            <tbody>
                @* @foreach (var obj in Model.OrderBy(u => u.Name))
                {
                    <tr>
                        <td>
                            <a asp-controller="Products" asp-action="Upsert" asp-route-id="@obj.Id" class="text-black">
                                @obj.Name
                            </a>
                        </td>

                        <td>
                            @obj.Category.Name
                        </td>
                        <td>
                            @obj.Provider.Name
                        </td>
                        <td>
                            @obj.Price
                        </td>
                        <td>
                            
                        </td>
                        <td>
                            @obj.Status
                        </td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Booking" asp-action="Book" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                     Book
                                </a>
                                @* 
                                <a asp-controller="Products" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </a> 
                            </div>
                        </td>
                    </tr>
                } *@
            </tbody>

        </table>
        <div class="card-body my-4">
            <div class="row">
            </div>
            <div class="row mb-3 pb-3">
                @* <div class="col-md-2 offset-md-1">
                    <a asp-area="Customer" asp-controller="Book" asp-action="Index" 
                        class="btn btn-outline-primary text-uppercase mb-5 btn-sm"><small>Continue Booking</small>
                    </a>
                </div> *@

                <div class="col-md-10 offset-md-1">
                    
                </div>
            </div>

            <div class="card-footer bg-white border-0">
                
                <div class="row">
                    <div class="col-md-4 offset-md-4">
                        <ul class="list-group mb-4">
                            <li class="d-flex justify-content-between align-items-center">
                            @*     <h5 class="text-dark fw-semibold text-uppercase"> Total: </h5>
                                <h4 class="text-dark fw-bolder">@Model.OrderHeader.OrderTotal.ToString("c")</h4> *@
                            </li>
                        </ul>
                    </div> 
                </div>
                
                @* <div class="row">
                    <div class="col-md-2 offset-md-5">
                        @if (Model.OrderHeader.OrderTotal == null || Model.OrderHeader.OrderTotal == 0)
                        {
                            <a class="btn btn-primary border-0 bg-gradient w-100 py-2 disabled">Summary</a>
                        }else {
                            <a asp-action="Summary" class="btn btn-primary border-0 bg-gradient w-100 py-2">Summary</a>
                        }
                    </div>
                </div> *@
            </div>
        </div>
    </div>
</form>
